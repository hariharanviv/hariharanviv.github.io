# Talks

This page lists talks and presentations. The map below shows locations where talks were given.

You can add talks in `talks/talks.json` with fields: `title`, `venue`, `location`, `date`, `lat`, `lng`, `slides`.
